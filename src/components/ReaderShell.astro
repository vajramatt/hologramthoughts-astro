---
/**
 * ReaderShell — Book-like container with parchment background and book shadow.
 * Replaces per-page `max-width: 800px` pattern with a consistent reading container.
 */
---

<div class="reader-shell">
  <slot />
</div>

<style>
  .reader-shell {
    max-width: 64ch;
    margin: 0 auto;
    padding: 3rem 2.5rem;
    background: var(--shell);
    border-radius: 2px;
    box-shadow:
      0 1px 3px var(--shell-shadow),
      0 8px 24px var(--shell-shadow),
      inset 0 0 80px rgba(0,0,0,0.02);
    position: relative;
    z-index: 1;
    min-height: 60vh;
  }

  @media (max-width: 768px) {
    .reader-shell {
      padding: 1.5rem 1.25rem;
      margin: 0 0.5rem;
      border-radius: 0;
    }
  }
</style>
